# 后台管理网页端运行指南

后台网页端位于 `admin-frontend/`，用于查看安卓端连接信息、教师数据、额度和作文批改记录。

## 1. 功能范围

当前是最小可用版，包含：

- 管理员登录
- 数据总览
- 安卓端连接检测
- 教师管理与手动加额度
- 作文批改记录查看
- 教师列表搜索
- 批改记录按关键词和状态筛选
- 多个可选局域网 IP 展示

## 2. 管理员账号

默认后台账号：

```text
用户名：admin
密码：admin123
```

当前账号写在后端 `AdminService` 中，适合课程演示和本地调试。正式使用时建议改为数据库管理员表和加密密码。

## 3. 启动后端

在 `backend/` 目录运行：

```bash
mvn spring-boot:run
```

后端默认地址：

```text
http://127.0.0.1:8099
```

后台接口路径：

```text
http://127.0.0.1:8099/api/admin
```

## 4. 启动后台网页端

进入后台前端目录：

```bash
cd admin-frontend
npm install
npm run dev
```

浏览器打开 Vite 输出的地址，通常是：

```text
http://127.0.0.1:5173
```

## 5. 连接其他后端地址

如果后端不是运行在 `127.0.0.1:8099`，可以在 `admin-frontend/` 下创建 `.env.local`：

```text
VITE_API_BASE=http://你的后端IP:8099/api
```

然后重启后台前端：

```bash
npm run dev
```

## 6. 安卓端检测页使用

后台的“安卓检测”页面会显示：

- 后端状态
- 当前服务端口
- 后端检测到的本机 IP
- 安卓端应该填写的服务器地址
- 当前电脑可能可用的多个局域网地址
- 上传目录是否可写
- OCR/AI 配置是否存在

安卓 App 登录页可以填写该页面显示的服务器地址，例如：

```text
http://192.168.1.8:8099
```

如果页面显示多个地址，优先选择和手机处在同一 Wi-Fi 网段的地址。一般家庭/校园 Wi-Fi 常见格式是：

```text
192.168.x.x
10.x.x.x
172.x.x.x
```

## 8. 后台权限说明

后台登录接口为：

```text
/api/admin/login
```

其他后台接口需要管理员 token。教师端 token 不能访问后台接口。

## 7. 常见问题

### 后台登录失败

确认后端已启动，并且后台账号为：

```text
admin / admin123
```

### 后台页面空白或请求失败

检查 `VITE_API_BASE` 是否指向正确后端，例如：

```text
http://127.0.0.1:8099/api
```

### 安卓检测页显示的 IP 不是 Wi-Fi IP

Java 获取本机 IP 可能拿到虚拟网卡地址。此时用 Windows 命令查看真实 Wi-Fi IPv4：

```bash
ipconfig
```

然后在安卓 App 中手动填写正确地址。

### 查看作文图片打不开

确认后端 `/uploads/**` 静态资源映射正常，并且图片文件仍存在于 `backend/uploads/` 下。
